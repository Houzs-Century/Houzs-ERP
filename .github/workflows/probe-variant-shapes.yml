# Read-only. Asks production why a named sales order is absent from MRP, by
# testing each of MRP's own demand filters against the row rather than
# reasoning about it from the screen. Writes nothing.
name: probe-variant-shapes

on:
  workflow_dispatch:
    inputs:
      target:
        description: "Which DB"
        required: true
        default: "prod"
        type: choice
        options: [staging, prod]
      doc:
        description: 'item code(s), comma separated, e.g. JAGER-(K)'
        required: true
        default: ""
        type: string

concurrency:
  group: probe-variant-shapes
  cancel-in-progress: false

jobs:
  run-staging:
    if: ${{ inputs.target == 'staging' }}
    runs-on: ubuntu-latest
    environment: Staging
    defaults: { run: { working-directory: backend } }
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with: { node-version: 22 }
      - run: npm ci
      - run: node scripts/probe-variant-shapes.mjs
        env:
          CODE: ${{ inputs.doc }}
          WH: ${{ inputs.wh }}
          COMPANY: ${{ inputs.company || 1 }}
          DATABASE_URL: ${{ secrets.STAGING_DATABASE_URL }}

  run-prod:
    if: ${{ inputs.target == 'prod' }}
    runs-on: ubuntu-latest
    defaults: { run: { working-directory: backend } }
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with: { node-version: 22 }
      - run: npm ci
      - run: node scripts/probe-variant-shapes.mjs
        env:
          CODE: ${{ inputs.doc }}
          WH: ${{ inputs.wh }}
          COMPANY: ${{ inputs.company || 1 }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
