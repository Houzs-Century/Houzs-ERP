# Inventory Ledger Divergence — Movements vs FIFO Lots/COGS COE (Correction of Error)

**Date:** 2026-07-25
**Trigger:** Owner opened the Stock Breakdown drawer for SKU `MAKOTO RC(S)-FVI BRONZE` (2990 SOFA MAKOTO RELAX CHAIR), KL warehouse, and found the movement ledger and the FIFO ledger disagreeing on the same SKU: the MOVEMENTS list ends at running balance **3** (5 IN − 2 OUT), but the FIFO lots sum to **4** and COGS shows only **ONE** consumption. Two OUT movements were recorded on the SAME delivery order (`2990-DO-2607-018`, 23/07 11:47 and 12:22, 35 min apart) but only one lot-consumption exists.
**Status:** Root cause TRACED against the code (static trace of `origin/main`; no prod query needed to prove the mechanism). A READ-ONLY DETECTOR sized the exposure across all SKUs (PR below).

**Go-forward engine fix SHIPPED** — `migrations-pg/0195_scm_fifo_out_short_batch_fallback.sql` (the OUT-branch batch→plain-FIFO fallback) merged 2026-07-25 as `75efc060`, PR #1261: *"stop FIFO OUT discarding its shortfall — sofa batch-key drift no longer diverges the ledgers"*. `deploy.yml` runs `pg-migrate.mjs` on every push to `main`, so it is **applied in production**. This line read "now built as a DRAFT … NOT auto-merged" until 2026-08-13, on a money-path migration that had been live since the day the COE was written — the worst kind of stale, because a reader plans around a fix they think is still pending.

The **historical data-repair backfill** (`backfill-fifo-divergence.mjs`) is the part that remains DRY-RUN by default, staging-first and explicitly owner-triggered.

The specific MAKOTO symptom is also logged in `BUG-HISTORY.md` (HIGH).

---

## 1. Incident — what the owner saw

The Stock Breakdown drawer for MAKOTO RC(S)-FVI BRONZE (KL):

- **MOVEMENTS (4):** IN +3 (`2990-GRN-2607-010`, 13/07) → running 3; IN +2 (`2990-GRN-2607-017`, 20/07) → running 5; OUT −1 (`2990-DO-2607-018`, 23/07 11:47) → running 4; OUT −1 (`2990-DO-2607-018`, 23/07 12:22) → running 3.
- **COGS (1 consumption only):** 23/07 11:47, `2990-DO-2607-018`, −1, RM530, from lot `2990-GRN-2607-010`.
- **STOCK LOTS (owned):** 2 remaining from GRN-010 + 2 from GRN-017 = 4. **TOTAL QTY (OWNED) = 4.**

So the MOVEMENT ledger (`inventory_movements`, signed sum) says **3 on hand**; the FIFO ledger (`inventory_lots.qty_remaining`) says **4 on hand**; and the COGS ledger (`inventory_lot_consumptions`) has consumed **1**, not 2. Two OUT movements exist, one consumption. The two ledgers that must be one truth are off by exactly one unit — the second OUT decremented the running balance but consumed no lot and booked no COGS.

## 2. Root cause — traced against the code, not guessed

**The FIFO engine is per-row and correct; the fault is that the OUT branch SILENTLY DISCARDS a short, and a resync delta OUT can produce a FALSE short even when stock is on hand.**

**The write path as it was on 2026-07-25 (all cited against `origin/main` at the
time).** Migration 0195 changed the OUT branch two days later and is live in
production — see §3. Read this section as the diagnosis, not as today's engine;
the paragraph after the bullets says which part still stands.

- **The only consume path is the AFTER-INSERT trigger.** `writeMovements` does a plain `INSERT` into `inventory_movements` (`backend/src/scm/lib/inventory-movements.ts:116`); there is NO JS-side consumption. Every OUT's COGS is produced solely by `trg_inventory_movement_fifo` → `fn_inventory_movement_fifo` (`backend/scripts/scm-schema/inventory-fifo-trigger.sql:267`, AFTER INSERT FOR EACH ROW).
- **The OUT branch always consumes — and DISCARDS the shortfall.** The OUT branch calls `fn_consume_fifo` (plain) or `fn_consume_fifo_batch` (sofa/dye-lot) and then only persists `total_cost_sen = v_result.total_cost_sen` onto the movement (`inventory-fifo-trigger.sql:176-198`). The `qty_short` the consumer returns is **thrown away**. `fn_consume_fifo` matches lots on `(warehouse_id, product_code, variant_key, qty_remaining>0)` ORDER BY `received_at, id` (`:55-63`); `fn_consume_fifo_batch` ADDITIONALLY requires `batch_no = p_batch_no` **exactly** (`:118`). If the loop matches ZERO lots, its body never runs: `v_remaining` stays = qty, it returns `total_cost_sen = 0`, `qty_short = qty`, inserts NO `inventory_lot_consumptions` row, and decrements NO lot. Result: the OUT movement is a real row (the signed balance decrements) but the FIFO ledger is untouched — a permanent movement-vs-lot divergence of exactly the shorted qty, with COGS missing for those units.
- **The first ship is idempotent, so the second OUT is NOT a re-post of it.** `deductInventoryForDo` guard #1: if ANY OUT movement already exists for the DO, it no-ops (`backend/src/scm/routes/delivery-orders-mfg.ts:834-841`). The first-ship path physically cannot write a second OUT.
- **The second OUT comes from the delta-resync path.** The only other path that writes a DO OUT is `resyncInventoryForDo` (`delivery-orders-mfg.ts:1141`), invoked when a SHIPPED DO's lines change (qty increased / line added). It computes `delta = target_qty − current_net_out` per bucket and, when `delta > 0`, writes a fresh DELTA OUT of `delta` units (`:1256-1269`) — deliberately a new INSERT so the AFTER-INSERT trigger fires (`:1131-1136`). The MAKOTO second OUT at 12:22, 35 min after the 11:47 first ship, is exactly this: the DO was edited to ship one more unit, and the resync wrote the +1 OUT.

**The mechanism, put together.** The resync DELTA OUT at 12:22 went through the trigger, whose consumer matched **zero open lots** for that OUT's consume key — so `fn_consume_fifo`/`fn_consume_fifo_batch` returned the whole unit as `qty_short`, the OUT branch discarded it, and the movement landed with `total_cost_sen = 0`, no consumption, no lot decrement. Because 4 units (GRN-010 = 2, GRN-017 = 2) were physically on hand at 12:22, this was a **FALSE short**: stock existed that a matching consume would have eaten. MAKOTO is a **SOFA**, so its OUTs route through `fn_consume_fifo_batch` (exact `batch_no` match); the most likely trigger for the zero-lot match is that the resync delta OUT's resolved `batch_no` (or `variant_key`) did not equal the open lots' — the "sofa/bedframe legacy POS-vs-Backend key + batch" fragility already flagged as a separate noted risk in `docs/inventory-costing-oversell-coe.md` §4. The first OUT matched (it consumed GRN-010 for RM530); the resync-time key resolution diverged for the second.

**Tool that proved it:** static trace of `origin/main` — `writeMovements` is the sole insert path (`inventory-movements.ts:116`) and the trigger is the sole consumer (`inventory-fifo-trigger.sql:267`); the OUT branch discards `qty_short` (`:193-198`); `deductInventoryForDo`'s guard #1 rules out a first-ship double-post (`delivery-orders-mfg.ts:841`); `resyncInventoryForDo` is the only other DO-OUT writer (`:1256-1269`). The runtime SIZE of the exposure (how many SKUs, how many RM) is measured by the detector shipped in §3 — production numbers land when the owner runs it.

**What of the above is still the live engine, re-read 2026-08-13.** The OUT
branch no longer routes a batch-stamped OUT to `fn_consume_fifo_batch` *or*
`fn_consume_fifo`: it runs the batch consume, and **when that shorts it falls
back to plain product+variant `fn_consume_fifo` for the residual**
(`inventory-fifo-trigger.sql`, the `IF v_short > 0 THEN` block in the OUT branch,
mirrored from 0195). So the FALSE short this incident is about cannot recur while
same-SKU stock is on hand. Everything else stands unchanged: `writeMovements` is
still the only insert path, the AFTER-INSERT trigger still the only consumer,
the first ship still idempotent, the resync delta OUT still the only other
DO-OUT writer — and the final `UPDATE inventory_movements` still persists only
`total_cost_sen` and `unit_cost_sen`, so **`qty_short` is still discarded** and a
genuine short is still detectable only by joining consumptions against qty.

## 3. Shipped (2026-07-25) — detection only, NO costing-logic change, NO data repair

| PR | What | Effect |
|----|------|--------|
| #1254 (shipped) | `backend/scripts/check-inventory-integrity.mjs` + `.github/workflows/inventory-integrity-check.yml` — a read-only reconciliation detector following the `check-soak-gate.mjs` / `check-uncosted-cogs.mjs` pattern (workflow_dispatch, own concurrency group `inventory-integrity-check`, `secrets.DATABASE_URL`, SELECT-only, exit 0 for every legitimate answer, results as `::notice::` annotations, schema/columns discovered from `information_schema` and re-validated). Two SELECTs: **(1) QUANTITY DRIFT** — buckets where the signed movement sum (byte-identical to the `scm.inventory_balances` convention: IN +qty, OUT −qty, ADJUSTMENT/TRANSFER +qty) ≠ Σ `inventory_lots.qty_remaining` (the MAKOTO 3-vs-4 case), listing SKU / movement-sum / lot-sum / delta; **(2) UNCOSTED OUT** — buckets where Σ(OUT qty) ≠ Σ `qty_consumed` in `inventory_lot_consumptions` (the 2-OUT-vs-1-COGS case), with an ESTIMATED RM exposure at the bucket's current open-lot weighted-average cost. Prints total drift-bucket count, total uncosted-OUT unit count, and total estimated RM exposure. | Sizes the exposure without an owner interruption or the DSN in front of a human. Touches no data and no costing logic. |
| #1261 (`75efc060`, MERGED + APPLIED — this row said "DRAFT — STAGING-FIRST" until 2026-08-13) | **Go-forward engine fix.** `backend/src/db/migrations-pg/0195_scm_fifo_out_short_batch_fallback.sql` (`CREATE OR REPLACE fn_inventory_movement_fifo`, mirrored in lockstep into the canonical `backend/scripts/scm-schema/inventory-fifo-trigger.sql`): the OUT branch (and the symmetric negative-ADJUSTMENT branch) now FALL BACK from the exact-batch consume (`fn_consume_fifo_batch`) to plain product+variant `fn_consume_fifo` for whatever the batch consume shorted. In-memory FIFO model + tests: `backend/src/scm/lib/fifo-out-consume.{ts,test.ts}`. | A sofa OUT whose `batch_no` drifts from its open lots' batch (the MAKOTO false-short) now consumes present same-SKU stock plain-FIFO, costs it, and decrements the lot — the two ledgers reconcile go-forward. Correctly-matched + plain OUTs unchanged; a genuine short (no stock) still uses the 0154 retro-cost path. Money-critical costing behaviour change → staging-first. |
| (fix PR, DRAFT — STAGING-FIRST, owner-run) | **Historical data repair.** `backend/scripts/backfill-fifo-divergence.mjs` + `.github/workflows/backfill-fifo-divergence.yml`: drives the audited, idempotent `fn_reconcile_uncosted_out` (0154) over EVERY divergent bucket the detector reports. DRY-RUN by default (each bucket's reconcile runs in a transaction and is ROLLED BACK — reports exact qty retro-costed / RM booked / lots decremented / drift closed, writes nothing); `APPLY=true` + a typed confirmation to commit; per-bucket transaction; idempotent (re-run books 0). Reconciliation invariant proven by `backend/src/scm/lib/fifo-divergence-backfill.test.ts`. | Repairs the already-diverged rows across all affected SKUs so Σ movements == Σ lot qty and Σ OUT qty == Σ consumed. Non-DO / drop-ship / cancelled OUTs and uncovered shortfalls are REPORTED for owner review, never silently changed. Writes only under an explicit, staged, owner-triggered APPLY. |

| (ledger-perfection, 2026-08-01) | **Two further modes on the same workflow** — the detector's remaining classes turned out to be TWO shapes `fn_reconcile_uncosted_out` is right not to touch. `MODE=relabel` (W4): the 2990 import copied movements and lots whose variant keys were computed by different writers, so per-bucket sums drift in equal-and-opposite pairs (the XAMMAR family; audit sections 1 + 2b) while every cost is already right — movements in DRIFTED buckets whose own consumptions (or own opened lot, `inventory_lots.movement_id`) all sit under ONE different key are relabelled to that key, consumption rows follow, lots and cost columns untouched, RM0 by construction; per-bucket before/after drift is projected in the dry run (`classifyMovementRelabel` + `projectRelabelledDrift`, `lib/ledger-repair-core.mjs`). `MODE=basis-cost` (W3, NAMED DOs only via the `dos` input): shorts that shipped with NOTHING on hand and no later receipt — the function is right to book nothing, so a REFERENCE-cost lot is seeded (newest same-SKU GRN landed cost in-company, else the product's latest PO line cost; `pickCostBasis`) and THE SAME 0154/0230 function consumes it and restamps the OUT. Refused when open lots exist (retro-cost is truer), when the seeded lot is not FULLY consumed by the named targets, or when any non-target movement draws on it — verified in-transaction, rolled back otherwise. The quantity drift of those buckets deliberately does NOT close (the units really shipped without a receipt) and the output says so. | The paired-drift buckets close at RM0; the named uncosted units carry an owner-approved reference COGS; nothing open-ended can write. |
| (ledger-perfection, 2026-08-01) | **Derived-layer restamp after the ledger repairs.** `backend/scripts/restamp-do-actual-cost.mjs` + workflow **Restamp DO actual cost (DRY-RUN gated)**: drives the REAL `restampDoActualCost` (and by default `restampSiFromDo`) over every shipped non-cancelled DO — reuse, not replication, over `scripts/lib/pgrest-shim.mjs` (a supabase-js-shaped builder on DATABASE_URL that throws loudly on any unimplemented method — the Actions environment carries no PostgREST secrets, proven by the 2026-08-01 live APPLY). Dry run prints a read-only staleness indicator (non-service line-cost total vs net movement cost per DO) plus an SI-vs-DO agreement report; APPLY prints every changed line old -> new. | DO lines and Sales Invoices read the corrected ledger; idempotent. |
| (ledger-perfection live-run fix, 2026-08-01) | **`MODE=reconstruct` — W4 phase 1.** The live relabel dispatch planned ZERO: every XAMMAR OUT refused `no-lot-evidence`, because the import dropped the `inventory_lot_consumptions` rows themselves (audit 2a: 13 lots with received − consumed != remaining; 10a: 13 unbalanced batches). This mode rebuilds the missing rows where the family's own arithmetic proves them — Sigma(movement shortfall) === Sigma(lot deficit) per (company, warehouse, product) family, FIFO pairing, per-movement cost honesty (stored === existing + paired -> pure re-link RM0; stored === existing -> stamped from the new rows, RM reported; else the family refuses and both sides print row by row). In-transaction re-plan CAS + family-conserves verification; dry run exercises everything and rolls back. | 2a and 10a close; relabel then has its evidence and closes 1 + 2b at RM0. |
| (document-evidence round, 2026-08-01) | **`MODE=doc-relabel` — the residual 1/2a/2b/10a resolved from the DOCUMENT chain** (owner rejected stocktake/manual: "为什么你不看 SO PO DO GR 去解决呢？"). The ledger-evidence modes refused correctly — the import destroyed the trail they demanded — but SO/PO/DO/GRN LINES carry their own `variants` JSON, maintained by a code path the import never damaged. Per (document, product) the family's ledger rows align to the document's own line keys computed via the LOCKSTEP `computeVariantKey` mirror (`planDocKeyAlignment` + `resolveDocLineKey`, lib/doc-evidence-core.mjs): DO line corroborated by its SO line, GRN line by its PO line; an empty-key side defers to the non-empty origin document; two non-empty keys disagreeing is `doc-conflict` and refuses. Relabels print the full citation (doc no + line + variants JSON -> key). The 2a over-consumed lots correct per document: DO net documented qty (lines minus delivery returns) must back every shipped unit, then the excess consumption re-points to the FIFO sibling receipt whose GRN proves the same goods (`planOverConsumedCorrection`) — donor decremented, 0154 cost convention re-verified in-transaction, RM delta reported. Only documents-disagree / no-document / no-receipt rows land on the printed STOCKTAKE list. | The residual buckets resolve on paper evidence; what remains on the stocktake list is genuinely uncountable from documents — expected small or zero. Drift left by still-uncosted OUTs closes when `MODE=retro-cost` re-runs after the relabel. |

Run the detector: Actions → **Inventory integrity check (read-only)** → Run workflow. The owner sequence for the fix + repair: (1) merge/deploy migration 0195, (2) run the detector to size it, (3) run **Backfill FIFO ledger divergence (repair)** in DRY-RUN and review, (4) point `DATABASE_URL` at STAGING and run with `apply=APPLY` + confirmation, verify, (5) prod with owner go. Nothing auto-applies. **The 2026-08-01 ledger-perfection sequence, as corrected by the live runs** (each step DRY-RUN first, then APPLY): part `ids` then part `dedupe` (owner-authorized removal of the duplicate-of-real pairs — a FULL-ROW twin on the real document licenses the delete; consumed lots restored; 2b bucket impact printed) then part `grn-gap` on **Repair 2990 doc references**, then here `MODE=reconstruct` (phase 1 — the audit-2a candidates verbatim, deficits paired + surpluses corrected) then `MODE=relabel` (phase 2 — now it has evidence; the paired drift closes at RM0), then `MODE=retro-cost` (an insert/relabel can hand real lots to previously-unrepairable shorts), then `MODE=basis-cost` for whatever named shorts remain, then **Restamp DO actual cost**, then the allocator recompute + the PO-drift check (W5b/W5c), then re-run the integrity check + costing audit to confirm the ledger reads clean.

**The 2026-08-01 document-evidence round (after the above applied; residuals 1=7, 2a=4, 2b=7, 10a=4):** on **Backfill FIFO ledger divergence** run `MODE=doc-relabel` DRY-RUN → review the per-row document citations and the stocktake list → APPLY → `MODE=retro-cost` (the relabelled OUTs can now consume the real lots) → **Restamp DO actual cost** (repointed/re-costed consumptions must flow to DO lines + SIs). Independently on **Repair 2990 doc references**: part `fifo-attribute` (the contended consolidated POs, FIFO attribution as allocation rows) and part `so-warehouse` (the header-unresolvable SO lines; mirror-owned company-2990 rows are reported for the 2990-side fix, never written locally). Then re-run the integrity check + costing audit — sections 1/2a/2b/10a must shrink to exactly the printed stocktake list.

**Live doc-relabel correction (2026-08-01, second dispatch needed):** the first live APPLY planned ZERO — every residual refused `no-document`, and the refusal keys exposed the real final class: **variant-key QUOTE DOUBLING** (`legheight=1""` vs the documents' `1"`; movement `8e9897ba` / over-consumed lot `f656b827`, the ARMANI EZ-008 family), plus a basis-cost repair seed (`b17d29da`) being reported as an unknown. doc-relabel now matches keys under a quote-doubling equivalence (`normalizeVariantKeyQuotes` — collapse doubled marks, trim trailing whitespace; the DOCUMENT's canonical key is always the relabel target, citation `key-normalized`), normalization-only relabels bypass the lens so a split bucket converges wholesale (post-plan consumption-edge check enforces exact final agreement, voiding what cannot converge), doc resolution falls back to the company-prefixed number, and `repair:`-marked rows classify `repair-seeded` in their own section so the stocktake list carries true unknowns only. The mint (hand-keyed pool data — no code in either repo appends a quote) is closed at the only in-repo door: maintenance-config saves now normalize the inch-bearing pools (`normalizeConfigInchPools`); `computeVariantKey` itself is deliberately untouched (re-keying history is the one forbidden move). Re-run the doc-relabel DRY-RUN after merging; expect the `1""` family to relabel with `key-normalized` citations and the stocktake list to shrink to genuinely undocumented rows.

**SO source-trace residuals (2026-08-01 live verdict: 132 lines, 99 ok/service, 33 unresolved), same PR:** `ready-no-open-lots` (19 stale READY flags) — dispatch **Recompute SO stock allocation (DRY-RUN gated)**, the canonical `recomputeSoStockAllocation` over the pgrest shim on `DATABASE_URL`, DRY-RUN executes-in-transaction-then-rolls-back printing per-line `stock_status` old -> new; stale READY flips to PENDING by the allocator's own rules. `no-do-line-link` (10 delivered lines) — part `do-line-link` on **Repair 2990 doc references** stamps `delivery_order_items.so_item_id` where the SO's own documents determine it 1:1 (split deliveries allowed, ambiguity refuses). `ready-lot-unbatchable` (4 lines / 9 lots) — accepted-explained: pre-0120/no-evidence lots; the trace check now labels them so, excludes them from the unresolved verdict, and names each remaining class's fix tool in its note.

## 4. What the audit RULED OUT

- **The second OUT is a duplicate re-post of the first ship — FALSE.** `deductInventoryForDo` guard #1 (`delivery-orders-mfg.ts:834-841`) no-ops when ANY OUT already exists for the DO. The first-ship path cannot write twice; the second OUT came from the delta-resync path (`resyncInventoryForDo`), i.e. the DO was edited after shipping.
- **The FIFO trigger swallowed a runtime ERROR — FALSE.** `fn_inventory_movement_fifo` has no `EXCEPTION` block; an error inside the AFTER-INSERT trigger would RAISE and roll back the movement INSERT, so the OUT row would NOT exist. The OUT row DOES exist and the first OUT (same DO) costed correctly — so the trigger was active and ran to completion. It consumed nothing by DESIGN (matched no lots), it did not error.
- **A concurrency race where the lots were momentarily 0 at 12:22 — FALSE.** The lots are present NOW (4 units; 2 of them GRN-010, which predates the DO), and `fn_consume_fifo` takes `FOR UPDATE` row locks; there was physical stock on hand at 12:22. A key-matching consume WOULD have found it. This is a consume-KEY mismatch (sofa batch / variant), not an availability race.
- **A genuine oversell (ship more than on hand) — NOT the trigger here.** On-hand was 4 and only 1 more unit was shipped, so the second OUT was not short of physical stock. It is the SAME discard mechanism the oversell COE documents (`fn_consume_fifo` returns `qty_short`, the OUT branch discards it) but reached by a FALSE short from a key mismatch, not by insufficient inventory.
- **A manual / legacy movement insert bypassing the trigger — LOW.** `writeMovements` is the single insert path and always fires the trigger; there is no trigger-disabled insert in the DO code, and the first OUT on the same DO costed correctly, proving the trigger was live for this SKU.
- **`restampDoActualCost` zeroed the cost — NOT the cause.** It only READS OUT/IN movements and copies their cost onto the DO lines (`delivery-orders-mfg.ts:529+`); it never writes movements, lots, or consumptions. It faithfully reflects the missing cost downstream — it does not create the ledger divergence.

## 5. DEFERRED — owner decides (intentionally NOT auto-done)

1. **The code fix — owner (money-critical FIFO change).** Candidate directions, each a change to the scm FIFO layer that lives directly in prod and is not fully reproducible from the repo, so per the migration-0154 discipline it MUST be validated on STAGING first and coordinated — do NOT merge blind: (a) stop the OUT branch DISCARDING `qty_short` — persist the shortfall (a column / flag) so an under-consumed OUT is self-evident instead of only detectable via the consumptions-vs-qty join; (b) fix the resync delta OUT's consume-key resolution so a sofa/variant OUT keys to the SAME open lots the first ship consumed (the FALSE-short root); and/or (c) a guard that refuses to record an OUT as costed-complete when it shorted against stock that is physically present. Not attempted in this PR.

   **Status re-read 2026-08-13 — one of the three shipped, two did not.**
   **(c) is CLOSED** by 0195: an OUT can no longer be recorded costed-complete
   while same-SKU stock sits under a different batch, because the batch consume
   now falls back to plain FIFO for its residual. **(a) is still OPEN and
   deliberately so** — `docs/inventory-costing-oversell-coe.md` §5 item 3
   records the decision to keep the shortfall LEDGER-DERIVED
   (`ABS(qty) - SUM(qty_consumed)`), because that is what makes every reconcile
   idempotent; the trigger's final `UPDATE` still writes only cost columns.
   **(b) is still OPEN**: 0195 works around the key mismatch, it does not fix
   the resync path's key resolution, so a batch-stamped OUT can still resolve a
   `batch_no` its own open lots do not carry — now costed, but still mis-keyed.
2. **Data repair of already-diverged rows — owner.** Once the detector quantifies the set, the owner decides whether (and at what cost basis) to retro-cost the uncosted OUTs and reconcile the movement-vs-lot deltas — same STAGING-first, coordinated discipline. Repairing money data is never an auto-fix.
3. **Reconcile MAKOTO specifically — owner.** The single reported SKU is one row in the detector's output; fixing it is part of the (2) data-repair decision, not a one-off hand edit.

## 6. Lessons

- **Two ledgers that must be one truth need a check that they agree.** `inventory_movements` (the running balance) and `inventory_lots`/`inventory_lot_consumptions` (the FIFO owned qty + COGS) are derived from the same events but by different mechanisms — the app's own on-hand views already sum them separately (`inventory_balances` vs the lot rollup). Nothing was reconciling the two until a human noticed a 3-vs-4 in a drawer. A read-only reconciliation is the cheapest possible guardrail for a money-critical invariant.
- **Discarding a shortfall hides a divergence forever.** The OUT branch throwing away `qty_short` is the same fault the oversell COE found — an OUT that consumes less than it ships leaves no trace on the movement it lives on. "Best-effort, will be reconciled later" only holds if the shortfall is RECORDED; a discarded short is invisible until the ledgers are cross-joined.
- **A FALSE short is worse than a real one.** The oversell COE's short was a real stock shortage awaiting replenishment. This one shorted against stock that was PHYSICALLY PRESENT because the consume key (sofa batch / variant) did not match — so it will NEVER self-heal, no replenishment is coming, the stock is already there. Key resolution on the resync path must match the key the first ship used, or every edit-after-ship risks a phantom uncosted OUT.
- **Size before fixing money data.** The safe first move on a money-critical divergence is a read-only detector (this PR), not a hot fix — it quantifies the exposure and hands the owner the numbers, with zero risk to the FIFO layer.
- **Verify against the pipeline, not the symptom.** "Two OUT, one COGS" looked like a duplicate movement; tracing the code showed the first-ship path is idempotent and the second OUT is a legitimate resync delta whose consume matched no lot — so the fix is in key resolution + shortfall handling, not in de-duplicating movements. The ruled-out list (§4) is what stops the next person chasing the phantom-duplicate theory.
