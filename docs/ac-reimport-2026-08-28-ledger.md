# AutoCount → ERP 重新导入（第二轮）流水账 — 2026-08-28 开账

> **这本账存在的理由是 owner 的原话（2026-08-28）：**
> 「记得 记录整个过程 可能我们这个也是 testing 然后之后 我们清洗了 再重新弄过。」
>
> 所以每一步都要能照着重走：跑了什么、run id 多少、数字多少、谁拍了什么板。
> 规矩沿用 `docs/autocount-cutover-ledger.md`（8 月第一轮的总账）：
> **历史行不改**，状态变了加新行；每个数字指得出出处；owner 原话原样保留。

## 0. 这一轮和第一轮（2026-08-09~11）的差别 — owner 拍板记录

| # | 拍板 | owner 原话 / 日期 |
|---|---|---|
| 1 | **SO 整张进**：一张单只要还有任何一行没送完，整张全部行都导入（含已送完的行），第一轮是只导没送完的行（245 行已送行缺席、39 张单头金额比 AutoCount 小） | 「如果是进整张，你就需要把它已经送货的 DO 也放进来 … ok」2026-08-27 |
| 2 | **DO 全配**：导入单的所有送货单都建镜像（不只部分送货的），照旧 `migrated_no_stock`、零库存流水 | 同上 |
| 3 | **GR→PI 链完善**：已收货 PO 配迁移 GRN；发票走"金额与 AutoCount 分毫相等才开"的镜像规矩（不重复记账、不动余额、不回推 AutoCount）；这一轮从一开始就带发票价钱，消掉第一轮"我们这边 RM 0"的 222 张拒绝 | 「有了 GR 之后，Purchase Invoice 也是要完善掉的」+「发票照旧走…镜像规矩 ok」2026-08-27 |
| 4 | **先清空再导**：ERP 的 HC 交易+库存清掉、主数据保留、号码继续往上（DOC_COUNTERS=keep，绝不归零） | 「而且你需要把我们目前的数据clear掉」2026-08-28 |
| 5 | **测试单永不回流**：所有导出条件排除 `DocNo LIKE 'HC-%'`（ERP 写回去的）和 `'ZZ%'`（QA 假单） | 本轮新规 |
| 6 | **每行收货量 = 行自己的 `PODTL.TransferedQty`**；聚合的 GrQty 这一轮**根本不导出**（第一轮它制造了 130 行假超收） | 第一轮教训，mig 记录 §9.1 |
| 7 | **新货号**：HOK-1056 六尺寸不开新 SKU，对到现有 `FLAT-(K/Q/S/SS/SK/SP)`；DL-CLASSIC 床褥 6 个、5562 沙发、RC-* 修理费 3 个 = ERP 开新 | 「bedframe我们有flat了 assign sku给hookka 然后放1056」「matress 没问题 sofa没问题 修理费也没问题把？就是我们系统要开进去」2026-08-28 |
| 8 | 冻结（write freeze）**保持全冻**直到导入完成（曾短暂开过"产品与设定"区，owner 说先不解冻，已冻回） | 「那就先不解冻把 我们继续migrate」2026-08-28 |

**待 owner 定（开着，别自己决定）：**
- ① ~~HOK 系列供应商绑定~~ **DECIDED 2026-08-28**：owner 原话「3两个都绑」+「hookka是main」——FLAT×6 和 5562-1S 同时绑 Hookka（400-H004，账本名 HAO HUA FURNITURE）和 OHANA（400-O002，账本 Item 主档的登记），**Hookka 为主供应商**。落地：`backend/scripts/data/ac-newskus-2026-08-28.json` + `align-seed-skus.mjs` 学会读 `is_main`（旧数据档不带 = false，行为不变）+ workflow 加 `data_file` 输入。对照表补 16 行（CSV 行数 1,561→1,577）。
- ② 账本里还 LIVE 的测试单（HC-SO-2608-003~008、HC-PO-2608-002~007、HC-GRN-2608-002~007、HC-DO-2608-004/005/007、HC-PI-2608-002~007、HC-SI-2608-006、ZZDIAG-SO-2）+ 2 个 QA 假货号（ZZ-ERP-QA-ITEM/2）——owner 自己在 AutoCount 清（cancel 或删）。不清也不影响导入（导出已排除）。

## 1. 清空（wipe）— 2026-08-28

| 步骤 | run id | 结果 |
|---|---|---|
| 预演（只数数） | 33143344805 | 会删 **93 行 / 68 张表**（= 8-22~25 的测试残留：SO/DO/PO/GRN/PI/SI 各 2 张 + JE 4 笔 + 明细）；号码计数器 6 个 HC 系列、keep |
| 正式执行 | **33143464728** | **93+4 行删除**（4 行是删除审计触发器回写、sweep 1 轮清掉）；核对全过：HC 交易表全 0、2990 逐表未动、主数据样本未动、出口记录 69 行保留（pending 1 → skipped）、计数器 6 系列原位（下一张 SO = 2608-009）；备份已上传 artifact（90 天） |

## 2. 货号核对（对照表 vs 整理后的账本）— 2026-08-28，只读直连

线路：本机 pyodbc → ZeroTier `10.147.17.100,55500` → `AED_HOUZS`（与第一轮同一条链）。

- 账本 Item 主档：**1,589 个**（active 1,467）；对照表 1,561 个**全部仍在，0 个消失**。
- 新增 28 个 = **16 个真新货**（DL-CLASSIC AURORA REST/DREAM VALLEY 各 3 尺寸、HOK-1056 六尺寸、HOK-5562 SOFA、RC-COM/CUS/TRP）+ **10 个我们自己写回开的沙发件码**（9028-1A(LHF) 等，ensure-masters 的脚印，不需处理）+ **2 个 ZZ QA 假货**。
- 名称是否改过**无法比对**——8-11 的旧快照没存 Description 栏（工具盲点，已如实记录，不当发现）。
- 供应商核实：HOK 前缀 = **OHANA STUDIO 400-O002**（整个 HOK 系列）；400-H004 = HAO HUA FURNITURE；400-D001 = Dunlopillo (M) SDN BHD；RC-* 无供应商（服务项）。

## 3. 新谓词点数（live 账本当场数）— 2026-08-28

| 项 | 第一轮 | 本轮 |
|---|---|---|
| SO（整张规则，剔除直开发票后） | 2,710 张 / 13,588 行 | **2,758 张 / 13,858 行**（含 296 行"已送完"新带入） |
| 直开发票剔除名单 | 129 张 | **2 张** |
| DO 镜像 | 59 张 / 275 行（partial only） | **72 张 / 310 行（全部）** |
| PO lane1（自己没收完，整张） | 157 张 | **188 张 / 429 行**（行数=未收行；整张导出行数见导出记录） |
| PO lane2（为未送 SO 开的，含已收完，整张） | 378 张 | **442 张 / 685 行** |
| 库存有数格子 | 1,020 | **1,075** |

**顺手修正一个仓里的错**：`data/autocount-refetch-po.sql` 写的 `pod.FromDtlKey` 在账本里**不存在**
（该 SQL 写了从未跑过）；实名 `FromSODtlKey`（sys.columns 验证）。导出脚本用实名。

## 4. 导出（export-ac-reimport.py，本轮新脚本）

一支脚本重写全部快照档（整份替换）：SO 整张 / iv-excluded / so-dates / PO 两路整张 /
全部 DO 镜像 / 库存余额 / 两个开账成本源 / GR+PI 参考索引（只限本轮 PO 集）/
PO→SO 行链 / outstanding 尺（ac-outstanding-now）。**不在本脚本**：库存分层
（ac-stock-layers，另一 pass）、照片 manifest、fidelity 真值（跑现成的
export-ac-fidelity-truth.py）、live 尺（export-ac-live.py）。

- 第 1 次跑：SO 档 13,858 行 ✅ 与点数一致；统计行因 Decimal 序列化炸掉 → 修（Decimal→float），重跑。
- 第 2 次跑（重跑全程）：SO 2,756 张/13,858 行（含已送完行 297）✅、iv-excluded 2 ✅、
  so-dates 13,858 ✅、PO lane1 188 张/500 行（整张口径：429 未收行 + 71 已收行）✅、
  PO lane2 442 张/685 行 ✅（**lane2 一段查询 23 分钟**）。跑到 DO 段时 **ZeroTier 断线**
  （08S01 semaphore timeout）→ 脚本加：每条语句断线自动重连重试一次 + `START_AT=<段名>` 断点续跑，
  DO 段从 2,756 个单号字面 IN 清单改成内联谓词（点数时同款几分钟跑完）。
- 第 3 次跑（`START_AT=dos`）：DO 72 张/310 行 ✅；库存余额 2,684 格（非零 1,074）✅；
  UTDStockCost 1,348 行 ✅。两个账本真相：
  ① **`DODTL.FromDocDtlKey` 在账本里整列全空**（310 行 0 个有值）——8 月档案里的 SoDtlKey
    是当时另行重建出来的；**导入端（create-migrated-documents 的 DO 半边）根本不读它**，
    配对走「SO 单号 + ERP 货号」按序消耗 + 沙发整组回退，所以无影响，栏位保留为 null。
  ② ItemUOM 没有 `RecentCost`，真名 **`MostRecentlyCost`** → 导出起别名 RecentCost，
    导入端读键（`RealCost || Cost || RecentCost`）不用改。
- 第 4 次跑（`START_AT=costs`）：**EXIT=0，全套落地。** ac-item-costs 181 行（8 月档 944 行——
  旧档没按"任一成本非零"过滤，这版只留有成本的行，导入端取值顺位不变）；ac-gr-refs **980 行**
  （8 月是全账本 82,451 行；这版**只圈到本轮要导的 ~600 张 PO**——盖号脚本只盖导入的 PO，够用且快）；
  ac-po-fromsodtlkey 685 行；ruler（ac-outstanding-now）SO 2,756 / PO 188 / SO-linked 442。

**快照总表（2026-08-28 版，整份替换 8 月档）：**

| 档案 | 行数 | 备注 |
|---|---|---|
| ac-outstanding-so | 13,858 行 / 2,756 张 | 整张口径，含 297 行已送完 |
| ac-so-iv-excluded | 2 | 直开发票剔除名单 |
| ac-so-dates | 13,858 | PDate + 行交期 |
| ac-outstanding-po | 500 行 / 188 张 | 整张（429 未收 + 71 已收行） |
| ac-so-linked-pos | 685 行 / 442 张 | 整张，含已全收；每行 TransferedQty，无 GrQty |
| ac-partial-dos | 310 行 / 72 张 | 本轮起 = 导入单的**全部** DO |
| ac-stock-balance | 2,684 格（非零 1,074） | vItemBalQty 全量 |
| ac-utd-stock-cost / ac-item-costs | 1,348 / 181 | 开账成本一、二顺位 |
| ac-gr-refs | 980 | 收货/发票索引，只圈本轮 PO |
| ac-po-fromsodtlkey | 685 | PO→SO 行链 |
| ac-reimport-manifest.json | — | 各档行数 + 口径说明 |

## 4b. 开货（新 SKU）执行记录

- PR #2747 合并（快照 + 种子档 + 流水账进 main）。
- 开货 dry-run 第 1 次（run 33152112108）：报 `create 3 / skip 17` —— **与只读导出（run 33152256221）矛盾**：
  live 绑定里 20 条一条都不存在（FLAT 只有 NB 六条，5562/DL 全无）。查明：种子档绑定键写了旧名
  `material_code`，现行脚本读 `item_code`（batch-3 命名统一后改的）——每家供应商首行占空键、
  其余 17 行被误判重复；apply 会插 3 条空货号垃圾。**dry-run + 对账拦住了**（docs/bugs/0554）。
- 修正：键名改 `item_code`（fix/ac-newskus-item-code）。
- **发现现成机制**：`mirror-hookka-bindings`（8-25 建）= 把 OHANA 每条绑定镜像到两家 Hookka
  （H003 制造 + H004 实业）并把主供应商统一提到 H004 —— owner 8-09 的老规矩（"两家一样 SKU
  一样价钱；Hookka Industries 有的都是 main"）。**开货 apply 之后跑一次它**：H003 自动补齐、
  FLAT 上 NB 的 main 让位给 Hookka（避免双 main）。

## 4c. 沙发轮 dry-run + 解析器学新写法（2026-08-28 下午）

- 非沙发 SO **全量导入完成**：limit=5 试插（5 单/18 行/5 款）→ 全量 run 33154299948：
  **2,287 单 / 12,934 行 / 2,205 笔付款**，81 条例外全部=源头颜色写残/未选（老类别，行照进颜色留空）。
  直开发票剔除 = 空操作（run 33154425027：名单 2 张、ERP 里 0 张——导出端已排除，双保险一致）。
- 沙发 dry-run（run 33154428381）：469 张全进，**432 组自动拆 / 121 组占位**。占位构成：
  3 行缺件 SKU（00913-3S ×2、8050-STOOL ×1）+ 45 行源头无结构 + 其余为**新写法被保守闸门拦下**。
  按 owner 规则（没 processing date 的缺 variant 没关系）：121 中只有 **32 行在已处理单上**是真工作量。
- **发现：店里 8 月中起换了 Desc2 写法**（SO-0131xx 起）：ERP 词汇 `1A(LHF)+C+2A(RHF)`、
  裸端链 `2A+1A(30')`、颜色在前无 COL: 标签。8 月语料没有这批写法，词表没有 A 系件词。
- 修（feat/sofa-parser-colour-first）：① 词表学 A 系（A+边→既有 E 记法；裸 A 按 owner 草图规则
  两端定边，**行中裸 A 仍然拦**）；② `1B` 词表本来就有（"owner: 1B 我们有"），不必问；
  ③ 颜色维持 #1998 契约——色库确认才收（试过"按位置照抄"，被钉契约的测试拒绝，**撤回**）；
  ④ 真正的洞：**三个导入脚本从来没把色库确认口子传给解析器**（docs/bugs/0555），补上（4 个调用点）；
  ⑤ owner 拍板开 2 个件 SKU（00913-3S、8050-STOOL），加进种子档（products 10→12）。
  10 条真实新行探针从"全拦"→ 9 high + 1 medium 全拆对；141 测试绿；light 全套除 3 个已知
  本机 CRLF 假红（CI Linux 同套绿）外全过。

## 5. 待办队列（照第一轮 19 步序，按本轮口径改）

导出全部落地并核对 → 对照表补 16 行 + 开新 SKU（等 owner 定 ①）→ 导入 SO（non-sofa → sofa）→
删 IV 名单（2 张）→ PO lane1 → lane2 → topup → 库存余额 → 分层 → 沙发库存 → GR/PI 号 stamp →
行键 backfill ×2 → PO 行键 → dedication → 纯损失修复 → 文本链 → ac_item_code seed →
迁移 GRN/DO 建档 → 迁移发票（金额相等规则）→ allocation 重算 → 验证五连。
每步 dry-run 先行、看 notice 不看徽章、APPLY 后独立重读核对——三条全是第一轮的铁律。

## 4d. PO 两路执行（2026-08-28 下午）

- **第一路（自己没收完）APPLY 完成**：run 33157041761 前段 + 重读确认——187 张 / 559 行进 ERP，
  例外 4 条照实（PO-009979 无码枕套行人工补；3 条 taroni 色词照旧留空）。
- **第二路 dry-run**：293 张新建（442−149 与第一路重叠）/ 511 行 / 连回 SO 497 行 / 带收货量 591。
- **第二路 apply（run 33169457109）中途崩**：PO-009555 的供应商 400-R002（RENNES BEDDING）在 ERP
  没有对应行，脚本只数数不跳单，空供应商撞非空约束整跑中断（docs/bugs/0557）。护栏补上（缺主档=
  跳该单点名报告，与第一路一致）。**身份问题挂 owner**：账本 400-R001 现名 RED SOFA PLT（23 个 RDS
  货的主供应商都登它），ERP 的 400-R001 却叫 RENNESS BEDDING，账本另有 400-R002 RENNES BEDDING——
  一家改名还是两家错码，owner 定；导入只照抄账本补 400-R002 一行，不判合并。
- **owner 裁定（「应该不一样啊」）**：RED SOFA 和 RENNES 是两家公司——ERP 400-R001 那行是
  **贴错名**（号对、名错）。修法 = 改名为 RED SOFA PLT（号与绑定不动；本批有 2 张 RED SOFA 的
  PO-000425/001068 会因此显示正确），工具 repair-supplier-names（plan 默认 + 确认句 + 独立复核）。
## 4e. 日期两个框不亮 — owner 在 HC-SO-013097 上抓到的（2026-08-28）

账本明明有（UDF_PDate 2026-08-17、行交期 2026-09-24），画面两个日期空。查实：
① 导入把 processing date 写进了 **8-18 就退役的旧栏 proceeded_at**（0286 改名后画面只读
`processing_date`，导入脚本没跟着搬家）；② 送货日期本来就属于补日期那一步，本轮还没跑，
而补日期脚本写的也是旧栏。两个脚本一起搬正（docs/bugs/0556），防呆名单保留旧名匹配。
账本里该亮的：processing 538 张、delivery 545 张（2,756 张里）——补日期 apply 后逐一核对。
另：PO-009979 的无码行「ERGOTEX PILLOW CASE - FAIR ×20 @RM50」照实例外，导入后人工补一行。

## 4f. 下午执行流水（2026-08-28，日期補跑 → 库存 → 行键）

跑一步、核一步，全部走 workflow_dispatch，run 号附在每条后面：

- **补日期 apply**：单头 551/551 张补上（processing / delivery 两个日期），行交期 2,528/2,528 行，
  人手改过的单 0 张被拒（本轮全是刚导入的，没人碰过）。owner 在 HC-SO-013097 抓到的两个空框，
  这一步之后亮了。（run 33170449326）
- **库存余额 apply**：981 格 / +9,699 件写入（其中 837 格暂零成本，等分层那步补成本）。
  独立复核 = 重跑一次 dry-run：**剩 0 格 / +0 件**，即账本余额已全部搬平。差成负数的 153 格
  （多是 DISPOSE 报废仓）只报告不写——ERP 不记负库存，属预期。5 个货号没绑定（枕套例外那批）。
  （apply run 33173101230，复核 run 33174030232）
- **沙发库存 apply**：116 批 / 116 件开仓批次写入，成本按账本批次成本。allocation 重算之后
  沙发行才会亮绿——这是设计，不是漏。（run 33173822826）
- **GR/PI 号 stamp**：316 张已收货 PO 盖上账本的收货单号+采购发票号（只写参考号，不建收货、
  不动库存——收货镜像是后面「迁移建档」那步的事）。（run 33174196073）
- **行键 backfill（沙发）apply**：782/782 行连回账本行；47 行账本找不到对应行、39 行数量对不上
  ——照规矩不猜，留名单。PO 侧 0 行（沙发 PO 行第一路已带键进来）。（run 33175468166）
- **行键 backfill（普通）apply**：~13,400 行，跑的时候本节在写，结果另起一条补记。（run 33174466591）

**一个假警报，记下来免得下轮有人上当**：诊断工具「PO 收货核对」在这个时点必然报红
（HC-PO-* 显示已收货、ERP 里却没有收货记录），因为收货镜像还没建——它建议重放旧的重算迁移，
**千万别跑**，跑了会把账本抄来的已收数量清零。等迁移建档跑完它自然转绿。（run 33174815264）

## 4g. 傍晚执行流水 + owner 的 Excel 抽查（2026-08-28）

- **层次成本 apply**：674 格重铺成 1,107 层真实成本，4,245 件里 3,970 件拿到账本的进货成本；
  7 格枕头数量对不上照实跳过、110 格无平铺批次不动。（dry run 33176855894，apply run 33176943948）
- **文本链修复**：本轮进的链路 0 需修（整单导入的策略把链一次带齐了）；顺手把 HC-PO-000290
  一条挂错行的链重指到正确行，独立重读 1→0。（run 33177071750）
- **ac_item_code 播种**：23 行可播全播，fresh 复读 23/23；1,067 行供应商目录号照旧留给人工。
  （run 33176792210）
- **收货镜像（GRN）**：316 张建档完成，不动库存（快照已含历史收货）。（dry 33178697194，apply 33178796412）
- **owner 用他的 SO listing（stocks status.xlsx）抽查，抓到本轮第二个漏栏缺口**：Remark 2
  （他的每单库存状态：READY 234 / READY (PARTIAL) 53 / BEDFRAME 50 / ACC 45 / BEDFRAME/ACC 26 /
  MATTRESS/ACC 17 / MATTRESS 3）、Remark 3（73 张）、Remark 4（211 张）、Note、还有他当送货日期用的
  那个日期栏（539 张）——导出根本没拉这五栏（docs/bugs/0559，与 0556 同类）。两个查证防止了两个错抄：
  listing 的「Note」其实是 UDF_Note（纯文字 481 张）；SO.Note 全账本只有 2 张有值、都是 RTF 内嵌**图片**，
  抄进来就是乱码——不搬。当天补齐：导出加节 → 导入 at-insert（#2763）→ 补写 dry 937 → apply
  **937/937**，fresh 复读逐字节相符（run 33178979475）。
- **对数核对工具上线并全绿（owner 问「数据也对？」的正式回答）**：8 个字段逐张对值 vs 本轮快照，
  **全部 0 差异** —— processing 538、customer 送货日 545、日期栏 537、remark2 428、remark3 73、
  remark4 210、note 481、行交期 2,528 行。（工具 #2764，run 33179652338）
- **账本漂移量测（快照 12:16 → 21:40 直连再量）**：processing date **0 张变值**；送货日期**当天被
  员工改了 40 张**（正常营运，天天在重排）。含义：抄写零错 ≠ 与「此刻账本」一致。**切换日要跑一次
  尾差同步**（现补写只填空、不改值——设计如此，人手改的不能被盖）。
- **行键 backfill（普通）**：~13,400 行逐行 UPDATE，跑了 100+ 分钟（写稿时仍在收尾；沙发轮 782 行
  5.5 分钟同速）。这是下一轮最大的省时点，见复盘 ①。
- **OPEN，别乱跑**：`repair-pure-losses` 的 dry-run 说 HC-PO-009736 五行 AKEMI 的已收数量要 3→0
  （它抄 PODTL.TransferedQty）。但我们导入时同样抄的 TransferedQty，怎么会不同？**矛盾即发现——
  没查清来源前不 apply**。GRN 镜像已建，receipt-drift 诊断的假警报（§4f）应已消退，重跑确认。

## 复盘：下一轮怎么省时间（owner 2026-08-28「下一次看一下怎么可以做得到节省一点时间」）

本轮从 wipe 到写稿约 12 小时。可省的，按大小排：

| # | 本轮花掉 | 下一轮 | 状态 |
|---|---|---|---|
| ① | 行键 backfill 两条 ~110 分钟（13,400+782 行事后逐行配对，还有 47+39 行配不上要跳） | **导入时直接带行键**——SO 导入在拆行的当下就知道来源行，比事后猜准，跳过名单直接消失 | **已改**（本 PR：ICOLS+linked_ac_dtlkey，普通+沙发三个写入点全带） |
| ② | 两次「漏栏→当天补」各 1-2 小时（日期 0556、remark 五栏 0559） | 导入 at-insert 已根治（#2761/#2763）；对数工具（#2764）每轮跑一次当验收 | 已根治 |
| ③ | 首个 PR 三轮 CI 往返（generated artifacts 过期 + data-pinned 测试没重钉） | 首推前本地跑全套 gate（约 8 分钟），一次过 | 纪律，写在这里 |
| ④ | 导出中途补一节会把尾部全重跑（今晚误覆盖 2 个快照，git 恢复） | `ONLY=<节>` 只跑一节、其余不碰 | **已改**（本 PR，实测：只写 1 文件、快照目录零改动） |
| ⑤ | ~20 个 workflow 手工逐个 dispatch、逐个等（每个 1.5-4 分钟排队+跑） | 做一个总调度：整轮按序 dry→读 notice→apply→独立复读，一键跑完自动串 | 提案，约 1 天工，owner 定 |
| ⑥ | 相关小改分了多个 PR，各占队列位（每位 15-25 分钟） | 相关改动并 PR | 纪律 |
| ⑦ | 长跑监控超时断链、目录被重置各咬一次 | 监控统一 10 分钟预算到期续布；gh/git 一律先 cd 仓库根 | 纪律 |

另有两件切换日必做（不是省时，是漏不得）：送货日期**尾差同步**（本轮量到当天就漂 40 张）；
验证五连 + 用当天 fresh 导出跑对数工具。

## 4h. 深夜收官（2026-08-28 深夜 ~ 08-29 凌晨）——单据链、发票、照片、验证五连全落地

**行键收尾**：主 backfill 跑到 86%（11,616/13,417）时我按推算误判它挂了、取消重跑——
其实 watcher 缓存里就有进度行（教训入复盘 ⑧）。收敛式设计让代价只有 15 分钟：续跑
1,801/1,801 完成，独立 plan 复读 **0 剩**（14,343 行里 13,417 已连回；916 行账本无对应 +
12 行数量不符照实排除）。（续跑 33181625007，复读 33182609755）

**镜像三件套 + 库存不动的当场证明**：GRN 316 张（run 33178796412）→ DO 70 张 / 314 行 /
453 件（run 33180660876；SO-003186/006438 各 1 行 DO 独有的 FOC 枕头照实跳过，owner 定
要不要补无来源行）→ 立刻重跑库存对账：**0 格 / +0 件差异、负数格 153 个与建镜像前一模一
样**——回答了 owner「先库存后 DO 数量会不会跑掉」：镜像一件不扣，扣的那一下在账本结余里。
（证明 run 33181084910）

**发票（金额一分不差规矩）三轮**：旧对照表 17 天陈旧（0561，晚上第 2 个 mtime-骗人坑；
账本 17 天里多了 985 销售 + 435 采购发票）→ 重拉+防呆后：第 2 轮开 **51 张**（采购 13 +
销售 38，fresh 复核全数镜像位、无日记账；run 33182730435）；盖价 547 行（0563 第 3 个坑修
后；274 PO + 273 GRN，run 33190902211）解锁第 3 轮再开 **15 张采购**（run 33191440271）。
**合计 66 张。** 剩余归档给 owner 选：166 张=一张账本发票盖多张收货、其余收货的 PO 不在
导入范围（规矩下天生开不了：不开/放宽/补导旧 PO 三选一）；86+24 张账本本来没开过发票；
2 张销售金额差（RM2,499 vs 2,549、RM6,538 vs 6,688）；6 组发票对应不唯一；15 行一票两价；
28 行仍无价。

**配货（READY 亮灯）**：Actions 借道的重算引擎坏在转接层（0562，保存点丢失+缺 upsert，
先绕后修）→ 走生产原生路：写队列单例修复请求行（工具 #2770），Worker 五分钟班车自己跑——
**READY 915 → 1,514 行（+599），PARTIAL 7→16，PENDING 13,421→12,813**。
（enqueue run 33183013211，回读 33183930541）

**对数全绿**（owner 问「数据也对？」的正式回答，工具 #2764 常驻）：8 个字段逐张对值 vs
本轮快照 **全 0 差异**——processing 538、送货日 545、日期栏 537、remark2 428、remark3 73、
remark4 210、note 481、行交期 2,528。（run 33179652338）账本漂移另量：processing 0 变、
送货日期当天被员工改 40 张 → 切换日尾差同步仍是必做。

**手写状态 vs 系统算的状态**（工具 #2775）：428 张手写里 **110 张全对上、97 张部分、
22 张写 READY 系统一行配不到**（名单在 run 33191340725）。主嫌疑=下一条。

**本轮最大遗留发现——迁移库存无规格 vs 订单行要规格**：readiness 显示 25 个床架 SKU
「有货但全是无规格、行要规格」（JAGER-(Q) 需 22 件、库存 36 件全无规格），reconcile 有
35 行「收满了还不亮」同因。owner 三选一：迁移批次放宽规格匹配 / 给批次补规格 / 保持现状
人工判。**未定前不动手。**

**照片回挂（不动云端一个字节）**：744 张 JPEG 只在 R2（本机 0 张、抽取器没留）、导入脚本
会铸指向不存在对象的新键 → 从 round-1 挂图 run 日志恢复旧键（新轮优先），按键里自带的
单号+行键匹配 `linked_ac_dtlkey` 原样回挂：**SO 401 + PO 97 = 498 张挂回**，抽样复读带键。
194 个键落在无键行（916 行名单的子集）+ 112 个旧轮存疑键（本机 wrangler 是个人账号、无
R2 读权限，量到 403）= 等 owner 重授权后验证再挂。另：照片清单内容是 8-10 的（晚上第 4 个
mtime 坑），8-10 之后账本新贴的照片不在册——补册要走逐行抽取通道，挂 owner 排期。
（工具 #2775，plan 33191337692，apply 33191493069）

**验证五连**：stage0 绿（防重键齐、0 重复送货桶）；readiness：header READY_TO_SHIP 176、
行 READY 1,514（40 行收货已到未亮=规格缺口类）；reconcile：应为 0 的「收满未亮」=35（同
上因）；parity 的逐单清单与 97-部分一致；**stock-vs-autocount：957 格比对 AGREE 891、
34 格差=账本当日又动（切换日尾差）、AC-only 31、ERP-only 1，件数 9,743 vs 9,698**。
（runs 33191344612 / 33191348540 / 33191352089 / 33191355509 / 33191359153）

**PO 行修复收敛证明**：dry-run **0 行要写**（266 行缺 so_item_id=账本本身无链接、5 组同货
同规双行拒猜）——本轮导入时带齐的策略在 PO 侧全验证。（run 33189216543）

**队列卡位一次**：#2773 入队 55 分钟 BEHIND 不动，按仓库既定补救本地并 main 推回解开。

复盘表补三行：⑧ 取消长跑前先翻 watcher 缓存的进度行（本轮 86% 误杀，代价 15 分钟）；
⑨ wrangler 4.x r2 默认打本地模拟器，`--remote` 才是真仓库（112 键全报 missing 的假象）；
⑩ 队列 BEHIND 超过 ~40 分钟 = 本地并 main 推回，不再等。

## 4i. Owner 定案：硬绑定 + 核对协议（2026-08-29 上午）

**硬绑定定案（他连发四条，全部与 8-10 已建成的 bound mode 一致，代码零改动）**：
① Company 1 的沙发+床架，SO 转 PO 即绑定成立——行对行、专属、别单不可碰；
② 收货按行、收几件亮几件（订 3 收 1 = PARTIAL(1)，代码 `min(got, need)` 已然如此）；
③ 床垫/配件不绑，照 2990 大锅按库存配（本无规格）；
④ 不做 2990 式软配——**B 方案（迁移单吃无规格旧库存）被否决撤回**：没自己 PO 的旧库存
不自动配单，等采购开 PO 或出货时仓库自选；旧货靠出货冲销，新货带规格，规格齐了整体转
2990 模式。

**核对协议（他定的验收顺序）**：先验我们自己的状态算得对不对，再跟 AutoCount 手写的
Remark2 对比；不一致**先过三道合理差再判算法错**——(1) 货已送掉（手写过时；一天 40 张
送货日期被改是同类证据）(2) 单没自己的 PO（硬绑定口径下本该不亮）(3) 一单多行我们逐行
算、手写指主件。三道排完剩下的 = ALGO-SUSPECT，**必须为 0**。

**落地**：对照工具升级为自动分类（DELIVERED-STALE / NO-OWN-PO / GRANULARITY /
ALGO-SUSPECT，最后一栏必须 0）；reconcile 的「收满必亮」镜头补上已送排除——先前那 35 行
里 SO-003295/009585 均为账本已送（连镜像都建了），镜头指控配货引擎"服从送货"为缺陷，
镜头错，不是引擎错。探针（#2777）把 35 行全量分类，未送而卡的（SO-004725/008942 类）
才进入引擎排查。

## 4j. 安静账本总同步(2026-08-29 下午)——owner 令:全程更新式,一样不删

Owner 指令原话要点:员工白天一直在动账本,现在没人碰了 → 整条流程**更新式**重走
(有的跳过、新的补进、被改的刷平、库存双向对平),然后重算、比状态、Remark2 差异归因,
全程记录。另三条确认:SO 开了 DO 要跟着建镜像、PO 收货要跟着刷、**绝不删 ERP 数据**;
DO/GR/SI/PI 必须都是链上 convert;Excel 不作数,Remark2 直读账本(而且他纠正:Remark2
是**侦测**出来的,不是手写——这句话改写了差异判读的口径)。

**新快照**(安静账本,20:0x 起全套重导):SO 2,750 / 日期行 13,846 / PO 两路 171+444 /
DO 源 73 张 316 行 / GR 凭证 1,025 / 库存 1,086 格 / Remark2 现存 414 / 发票对照+价目
in-tree 生成器重切。快照 PR #2787 一度被必需检查拦下——**编目没跟着新快照重生成 +
五处数据钉死测试没重钉**(复盘③自己写过又踩,统统当场补:corpus 697 行重生成,钉值
938→941 / 685→696 / 桶 7→9 行 14→18 键 5→7,样例单 PO-009830 完结出册改钉 PO-010093)。

**更新流水(每步 dry→apply→独立读数)**:
- SO:**+14 张 / 82 行 / 14 笔付款**,2,736 张原样跳过(新单出生自带日期/状态/行键)(run 33251287997)
- PO 一路 **+14 张/18 行**(33251359044);二路 **+5 张/8 行/7 绑定**(33252192421;
  PO-000254 旧式沙发写法 "1 ELT" 未识别→占位照规矩)
- 补漏 **+10 行/6 张 PO/8 绑定/10 行键**(33252569588;第一次 apply 被 CONFIRM 闸拦=闸在干活)
- 收货号补盖 **37 张**(33252333676);GRN 镜像 **+3**(33252461630);DO 镜像 **+1 张/6 行/12 件**
- 发票 **+1 张销售**(HC-I-2608-0072,fresh 复核;38 张已在跳过)(33252571041)
- 库存**双向**对平:**+21 格 89 件收 / −185 格今天送掉的按 FIFO 扣,合计 206 笔调整**
  (NEG=1 首次启用)(33252692216);沙发 **+5 批 5 件**(33252844616)
- 刷值(新工具 #2787):**90 张头**(送货日 80/processing 30/日期栏 32/remark2 21/r4 32/r3 1/note 1,
  账本清空的照清)+ **273 行交期**,0 张被人碰过,fresh 复核相符(33252926208)
- 行键:**0 剩**(13,405 已连;1,007 行账本无对应+13 组不可分照实;沙发补 2)(33253068095/33253106557)
- 配货重算走 Worker 班车(33253140476)

**终验(vs 安静账本)**:
- 八字段对数 **全 0 差**:processing 530 / 送货日 537 / 日期栏 525 / remark2 414 / r3 72 /
  r4 209 / note 480 / 行交期 2,491(另 90 行=账本送货后清空行日期而 ERP 留历史值,增量刷新
  只在账本有值时写,如实记录)(33253620406)
- **绑定必亮=0**(沙发修复在新数据上持续成立)(33253651846)
- 状态终局:**READY 1,440 / PARTIAL 12 / PENDING 12,973**(较早上回落是真实世界:今天送掉的
  货扣了、部分单结了、账本清了 30 张 processing date→闸住)
- 双向 Remark2 矩阵:**一致 2,541**;账本多 97(已送过时 28+无自家PO 6+粒度 59+无日期 2+
  **ALGO-SUSPECT 22**);ERP 多 72(账本写别类 63+账本空白 9)——ERP-多方向病因即
  「侦测器口径按整类/未跑到」,无害
- **遗留唯一悬案:22 张床垫类**(from 24;名单微变)——数量两边一致、字符逐字同、仓同、
  状态正常,员工实点四款床垫即可终审;若实点有货则回头深挖引擎(0562 的姊妹)

**过程教训(入复盘)**:⑫ 带确认句的 apply 在链壳里引号嵌套会哑火——确认句一律裸调;
⑬ 「已排队」≠「在队里」:mergeQueue GraphQL 查空即为空,先查自己单上的必需检查。

## 4k. 归零证明抓到的两个真缺陷,当天修复、当天归零(2026-08-29 傍晚)

对平后的例行归零证明(run 33254607183)没有读到 0,反而要 +1,255 件——**这正是它存在的
意义**。两个真凶:

- **0566(高)**:余额导入器按「账本行」逐行算差,而 35 个 ERP 格由多个账本码共同供给
  (DIVAN ONLY 四供应商家族、HOK/NB 床架双胞胎、两个枕头码…)。NEG=1(本轮首启)下,
  同格第二行把整格再扣一次:SQUARE PILLOW @ Balakong 真差 -173,被执行了 -173 和 -180,
  落到 **-161**。修复=先按(ERP 码,仓)聚合再一格一差。
- **0567(中)**:账本货号 30 字上限——新 Winter 系列的码生在上限上,右括号在账本里
  **本来就不存在**("DL-CS2 NN-WINTER SLEEP MATT (K" 恰 30 字,就是真码);对照表照抄了
  截断串当 ERP 码,还把 ARCTIC/NANO/FLOW/REST 四个真系列错并到 Winter 名下。17 行重建
  (各归各名、括号补全;四系列命名按家族式样,owner 可再改名),导入器新增「括号不平的
  目标码一律大声拒绝」。

**修复链**:#2789(过程中又补两刀:item-map 忘重生成→重生成;一条钉着坏数据当例子的
匹配测试→改钉治愈态)→ 修正版 dry:**73 格 / +613 件、负 0**(SQUARE PILLOW 合并为一格
+208 且带真成本 RM16.89;Winter 全落干净码)→ apply 73 笔(run 33256200346)→
**归零证明:正 0 / 负 0**(run 33256248116)。库存自此与安静账本逐格一致;唯余 5 个新
沙发拆件码(2376/9028 A 件,共 6 件)待补对照行。修复后追加一班配货重算。

复盘 ⑭:**双向对平(NEG)上线首日必须跟一发归零证明**——正向多年无恙不代表负向安全,
多码合一格这种老结构在负向是放大器。

## 4l. 终审:最后 22 张状态悬案结案(2026-08-30 凌晨)——两套算法的定义差,我们是对的一方

Owner 连环三问(顺序?location?SP?)逼出最终拼图。逐一验毕:

- **SP 排除**:22 张的原始行全为标准码(153x190/183x190),无 (SP)、无订制描述。
  顺手落地 owner 新裁定:**SP 订制床垫加入硬绑定**(#2792,红转绿测试 + 标准床垫不
  误入的护栏;docs/bugs/0568)。
- **排序验毕**:引擎排队钥匙=有效送货日(改期优先)→单号→建单时刻,与 MRP 共用同一
  日期读数器(代码共读,防口径分家)。
- **location 验毕并成为破案钥匙**:按仓摊开账本自己的供需——
  ULTIMATE (Q):KL 需求 188 vs 库存 12,PG 63 vs 9;IMMORTAL (K):KL 138 vs 15,
  PG 38 vs 4。**要的比有的多十几倍。**

**判决**:12 件库存点不亮 188 张单,这是数学。我们按日期先来后到亮到货尽为止=正确;
账本 Remark2 侦测器**不做分配**,只看"仓里有无此款"即打 READY=存在感算法,必然超发。
22 张不 tally 的根源是**两套算法的定义不同**,不是数据错——它们将随补货按日期顺序
自然亮起。仓库实点撤销(供需悬殊已数学定案)。

**自我纠错入册**:我核对工具的"剩余"估算(结余−已亮占用)被证伪——它在 12 vs 188 的
格上算出"剩 14",制造了 22 张假嫌犯。已改为诚实口径:**整格需求装得进整格库存、这行
却不亮,才算法嫌疑**;更细的问题只配引擎亲口重放(0562 嵌套支持排期)。复盘 ⑮:
**外部模拟引擎的算术,先过"物理可能性"再报数**。

**转接层修复实录**:保存点连坐死症根治(#2791:六路并发下 RELEASE 连带后续保存点;
串行化triads + shim 长出带测试的 upsert);借道重放随即撞出第二层(嵌套查询,设计域外),
留 0562 排期;Worker 生产路全程无恙。

## 4m. 2026-08-30 owner 三连问 + 全书关系互验round(记录=owner 五令)

**Owner 三问,全部以账本原始行答毕(probe-owner-docs.py,只读 pyodbc):**

1. **「沙发单全是 1S 1S?」** 账本沙发座位组成写在 Desc2 自由文本;上轮 547 行沙发
   拆件 444 成功、103 行按「绝不猜件」落 `{型号}-1S` 占位(remark 带 SOFA UNPARSED
   原文)。103 = **46 行账本根本没写组成**(HC-SO-013384/5 属此类,颜色/特制均已抽到,
   截图可见)+ **~57 行解析词汇缺口**。词汇修复 #2797(11 个金标全用真账本行,先红后
   绿 82/82;语料 71/71 未动):带数字的特制长词(HEADRESTCHANGETO8030 等)改判「随行
   特制」不再杀段;新增 1Console→1CT、1EFL/R、1R(P)、字母开头括号标题、1B/S seater、
   孤儿尺寸、型号随行、fabric 库确认的颜色 token。实测新解析器可机器救回 **~8 行**
   (008166/010015/010324/011530/012695/013226/013329/013121);其余 ~95 行账本真没写,
   要人看图补——名单待下轮导入 run 的解码日志出权威版后交 owner(补法二选一:门市在
   ERP 补,或账本补写后 resync 自动拆)。「1 ELT」歧义行的**拒绝**金标保住(开发中
   一度被裸 NA 臂破坏,当场撤臂)。
2. **「HC-SO-013338 好像送了,确定没 DO?」** 账本本身 DO=0、IV=0、TransferedQty=0
   (PROVEN)。不是漏抓——若货已实交,是门市没在 AutoCount 开单;下轮同步一开即跟进。
3. **「JAGER 没 PO 怎么 BedFrame ready?我们不是 hard binding?」+「没 processing
   date 怎么分配 accessories?」** 账本确实没有任何 PO 绑 SO-013367(0 行)——「未关
   联」显示忠实;亮灯是**显示合成层**越过引擎的两道闸:合成规则把「MRP 说有货」直升
   READY,而 MRP 按 SKU 合池,看不见 processing-date 闸(allocGated)也看不见按变体
   bucket 的 hard binding(70 件空变体 JAGER 老库存对不上有变体的行)。修复 #2796:
   `effectiveLineStockStatus` 第三参数 gates 必填(无日期不升格;床架/沙发/SP 床垫
   不靠 MRP 升格;存储 READY 永不被否),绑定判定 `isHardBoundLine` 从引擎导出共用,
   先红(6 失败)后绿,全套 532/533(唯一失败=既有 doStockLeavesOnConfirm 钉,主干
   同样红,另有任务卡)。bug 0569/0570 入册。付款题再答:5,199 = 10,399 − 5,200,
   余额=还欠,数字对。

**「不要只看一张 SO,全书互验、缺的补齐、全部可重跑」→ 本轮动作:**
- 新工具 `check-ac-erp-doc-links.mjs` + workflow(只读):账本关系图 vs ERP 关系图
  双向核对(五类单在册、SO→PO 行级绑定两个方向、DO 认父、账本已开票 ERP 还活着),
  快照 >2 天自拒;入手册阶段 4 第 18 条,backlog=0 为合格,非 0 即下轮补课名单。
- 手册照跑:阶段 0 三导出全新鲜(02:36-38;SO 2,750 / lane2 444 / DO 73 单 316 行 /
  库存非零 1,086 格 / Remark2 413;invoice-refs grToPi 5,217 / doToIv 10,396;价单
  控制组 98.3% agree)。corpus+item-map 重生成后 test:light 532/533,**零重钉**。
- 半路修一桩:#2794(14 短名改长名)卡在 ARCTIC 钉——CSV 改的是 ERP 侧名(对),
  测试输入还喂旧 ERP 名。账本 Item 主档实测 `DL-CS2 ARCTIC DREAM (K)`(无 MATT),
  钉的答案不动、输入跟改名走;顺手两次本地并主干解 BEHIND。旧看守链(bztiny1vp)
  零输出 >1.5h,按监管规则接管击杀,步骤改为本人直驱。

### 4m-补:互验 round 的执行数(跑完即录)

**连锁全程(2026-08-30 凌晨,均 prod,已核 job 名):**
SO 非沙发 2,287 全在册新增 0;沙发趟 2,750 全在册,解码 **452 拆件/95 占位**
(新词汇实收 8 行,95 里 ~92 行=账本真没写组成、其中 22 张已开 PO,名单在
门市补件清单);PO 一路 140/140、二路 444/444 全在册;topup MISSING=0;
GR 盖章 to stamp 0;镜像 GRN 319/319、DO 71/73(差 2 = duplicate-guard 故意拒:
DO-001800/DO-005583,其 SO 行已被别张 DO 认领);发票可开 0(319 refused=既有
金额不符/跨界类);**库存归零证明 正0/负0**;沙发批次 0 新增(117 在册);
成本盖章 +6 行(新连接读回一致);尾字段刷新 6 头+5 行交期;行键 to set 0;
重算入列→班车吃单→计数三读一致 PENDING 12,950/READY 1,463/PARTIAL 12(幂等)。

**终验:** 八字段 DIFFER 全 0;bound 必零=0;Remark2 双向矩阵 agree 2,546、
**ALGO-SUSPECT=0**(差异归因:已送过时 28/没自家PO 9/粒度 68/无日期闸 2);
reconcile item 镜头 506/516 对,10 差里 9 个=套码 vs 件码的表示差(平衡导入器
有映射所以 0/0),1 个真差=BASTION (SS) ERP 多 2 件、躺在账本没开行的仓
(疑展示仓,非本轮造成,处置呈 owner)。

**关系图核对器首战(0569 工具):** 抓到并归因——PO 缺 1=ERGOTEX(老项);
DO 缺 2=duplicate-guard;GR「缺 216」/PI「缺 150」=**核对器自己比错字段/未标注
拒开类**(GR 号在 purchase_orders.linked_ac_grn_docnos;PI 缺=exact-amount 拒开
的 owner 名单)→ 已修;SO→PO 边:67「行未导入」多为沙发套行的表示差(已分桶
不计 backlog),7 条边缺 = dedication 修复 lane 的 3 重指+1 补建+3 诚实跳过。
修复 lane 的 apply 被**它自己的全域护栏**拦下并回滚——护栏扫全公司而非本批,
存量一条 `PO-001696 2379-2S→占位 2379-1S`(沙发占位类)一票否决所有干净批次
= bug 0571,护栏改为「只拦本批新产生」,分支直跑(--ref)实测存量恰 1 条。

**流程雷(已写回手册):** dispatch 一律带 `-f target=prod`——SO apply 第一趟
整批落 staging(默认值),job 名 run-staging 才暴露;prod 未动。

## 4n. 2026-08-30 owner 追问「没 PO 怎么 Ready」→ 引擎抓出真暗道(bug 0572)

Owner 三连追问(走后门?考卷比了什么?Company 1/2 分了没?)全部成立到底:
**引擎绑定判定没分公司,且未收货的绑定行会掉进公池继续分**——之前"硬绑定成立"
只是因为有规格的行对不上空白规格的旧库存,是**碰巧**不是规则。

**实测(只读体检 check-bound-exclusivity,分支直跑抓现行):** 公司一 433 行亮灯
绑定行里 431 行=自己收货 PO 点亮(0 个错仓),**2 行现行**:HC-SO-013253
JAGER-(Q)(空白变体行 × 空白变体旧库存,公池暗道点亮,无任何 PO)+
HC-SO-000870 CODY-(K)(当晚绑定重指后的未重算旧灯)。公司二 21/21 全 PO 点亮
(巧合,非规则)。

**修复(#2802,先红后绿):** `HARD_BOUND_COMPANY_ID = 1` 出口在引擎;公池行走
对公司一绑定组强制 PENDING(公池永远不是它的证据),公司二照旧;沙发本就只认
批次/收货。红测试=生产原形(C1 床架+空白变体+公池有货+无 PO→修前 READY);
守卫:有收货照亮、公司二照池、标准床垫照池。10/10;全套 533/534(唯一红=既有钉)。

**部署后闭环:** 等 Deploy(backend success)→重算入列→班车吃单——计数应声:
READY 1,463→1,461(−2)、PENDING +2 = 恰好两行熄灯;体检复跑 **431/431 全部
自己 PO 点亮、无 PO 亮灯=0**;Remark2 矩阵 agree 2,546→2,548(+2)、ERP 超前
77→75、床架超前 6→4,ALGO-SUSPECT 保持 0——修完两边答案更一致,反证考卷
从来是独立对答案、没抄。

**给 owner 的直答已交:** 状态两边独立算、考卷 87 处差异+归因就是没抄的证据;
屏幕旧灯=显示捷径(0569 已闸);逐行「谁点亮的」这道题考卷原本没有,现在有了
(census 入手册,公司一「无 PO 亮灯」永远必须 0)。431 行的 PO 收货仓 vs 行仓
0 错配,回应他的「要分仓库」。

## 4o. 2026-08-30 owner「不可以超前,全部要对齐」→ 75 张逐行审 → 考卷自己的隐形字节案(bug 0573)

Owner 令:超前的 75 张逐张查"多了什么 category、找出问题、看怎么变 tally"。
逐行点灯来源审计(加进 remark2 考卷,永久保留):**RED(无正当来源)= 0** ——
没有一张是 JAGER 类"床架/沙发无 PO 亮灯"。来源:已送货 35 行(送掉的货,
存在感侦测器永远看不见)、公池现货 15 行、自家 PO 收货 5 行。

审计途中考卷自曝惊人冤案:多张单打印出「book="MATTRESS/ACC" 却算我们单方超前
accessory」的不可能形状。逐字符定案:claimSet 的正则里嵌着**一个隐形退格字节
(0x08)**在 ACC 前 —— 要求备注里先有退格符才认 "ACC",而没有备注含退格符,
所以账本的 ACC 缩写**从该模块诞生起从未被认过**。三层渲染全都伪装了它(裸打印
吞掉、JSON.stringify 显示成貌似故意的 \b 词界),只有 charCodeAt 终结争论:
`47,8,65,67,67,47`。CLAUDE.md「配不上的 checker 报干净」类的反向版:配不上的
模式**制造**了差异。

一个字节修掉,同数据重跑:**一致 2,548→2,591(+43),我们超前 75→16(−59 张
全是冤案:账本明明认领了 ACC),双向混合 7→0,账本超前 87→110(它的认领终于
被完整读到),ALGO-SUSPECT 保持 0**,账本侧四抽屉归因原封不动(那条路径从没用
过坏正则)。剩余 16 张:13 张账本侦测器根本没盖过章(多为远古已送完的单)+
3 张账本写了别的 category。调试打印(claim 分类、字符码)故意留在 SAYS-OTHER
输出里——下次这类隐形字节案一眼现形。

### 4o-补:「账本亮我们没亮」110 张拆到底(owner 追问"那些没解决?")

粗口径抽屉(GRANULARITY 68)按 owner 追问拆分为两格并逐行点名(run 33302806018):
**QUEUE-SHORT 34**(公池床垫在该仓不够分、按日期排队没排到——采购缺口,货号@仓
逐一点名,ULTIMATE(Q)/IMMORTAL(K)@BALAKONG/PENANG 为大宗)与 **COARSE-VOCAB 21**
(主件全备只差配件,账本 READY 只说主件)。NO-OWN-PO 在新口径下 9→22(混合单归
类更锋利),内部再分 "(无PO)"(约 10 张,沙发件为主——采购待开名单)与
"(PO未收货)"(到货即亮)。终局:28 送完+22 绑定待PO+34 排队缺货+21 口径+2 无
日期+3 类别形=110,ALGO-SUSPECT 保持 0。判词:110 张无一张是系统算错;51 张
(22+34)是真实的采购/到货事项,账本侦测器抢先打勾不足为凭。

## 照片管道 (2026-08-31)

第一轮把照片从账本里挖出来的那个脚本**没留下来**
(`docs/autocount-further-description-photos.md` §2.1),所以照片这条路一直是断的:
manifest 还在、R2 上的档还在、挂回的脚本还在,**唯独「再拿一次」做不到**。这次把
缺的那一半补上,四步一条线:导出 → 上传 R2 → 挂回行 → 验。手册在
`docs/ac-resync-runbook.md` 阶段 3b。

**两个新脚本**

| 脚本 | 做什么 |
|---|---|
| `backend/scripts/export-ac-line-photos.py` | 只读账本,把每一行的内嵌图抠成 JPEG + 写 manifest(`{DocNo, DtlKey, ItemCode, file, sha256, bytes, w, h}`,PO 多一个 `Desc2`)。按 DtlKey 记断点,断线重跑不重下。RTF 的解析**调用现成的** `further-description-rtf.mjs`(它带 `\*\blipuid` 和奇数 nibble 两个坑的守卫),没有另写一份 |
| `backend/scripts/upload-line-photos-r2.mjs` | 传 R2。**自己不算 key**——只认挂回脚本 resolve 模式印出来的 `UPLOAD <档> -> <key>`,并逐条比对第一轮的格式,对不上整批拒。四道闸门:默认 plan / apply 要确认句 / verify 是**另起一次**抽样下载回来比 sha256 / 头部写了重跑会怎样 |

**账本当场数出来的三件事(2026-08-31,只读直连)**

1. 带照片的行:**SO 2,723 行、PO 2,392 行**。第一轮的 manifest 只有 554 + 190,
   而且 SO 侧是从 DtlKey 34553 才开始的——**老单的照片绝大多数从来没被拿出来过**。
   (交接时以为 SO 只有 594 行,实测不是,以 2,723 为准。)
2. 图的形式**全部是 `\wmetafile8`**,jpegblip / pngblip / emfblip / dibitmap 全是 0。
   仓库里**没有** WMF 转点阵的现成代码(`rtf-picture.mjs` 自己写着 wmetafile
   `producible: false`),所以 `wmf_to_jpeg()` 是这次唯一的新解码:照第一轮写下的
   `WMF -> DIB -> JPEG` 路子,把 DIB 直接从 metafile 记录里抠出来重编码,**原始像素
   原始尺寸**,不经 GDI、不用挑 dpi。
3. **一行可以挂不止一张**:SO 最多 2 张、PO 最多 5 张。这条正好是
   `docs/autocount-further-description-photos.md` §7 问题 8 悬着的那个,答案是
   「不止一张」——**回写那条路要小心**,它是整个字段覆盖式重写,不先读回来就会把
   第二张抹掉。已回填进该文件。

**小样实证(SO,`LIMIT=20`)**:20 行 → 20 张,全 `wmetafile8`、全走 dib、0 失败。
其中 2 个 DtlKey 也在第一轮 manifest 里(34553、34737),**尺寸分毫不差**
(240x159、240x50)——这是新旧两条提取路等价的凭据。图打开是手绘沙发规格纸,
方向、颜色、字都正常。

顺带一个**差异,没去圆它**:DtlKey 34737 第一轮记的货号是 `HOK-2009(A) (K)`,
今天账本上是 `HOK-2008(A) (K)`(单号 SO-000383 两边一致,照片尺寸也一致)。
**LIKELY** 是账本那边改过货号,但没查证,先记着。

~~**还没做的**:整本 5,115 行一次都没跑过(只跑了 20 行小样),PO 侧一行没跑;
R2 一次都没传过(token 档还没建)。~~

> **全部做完了,就在 2026-08-31 当天,而这一段从没被回来改过 —— 更正于 2026-09-02。**
>
> - 整本跑完:SO 2,724 张 / PO 2,565 张,**failed 0 行**(PR #2816)
> - R2 上传跑完:04:16-04:58 UTC,840 个 key,在两趟挂回(04:44 / 05:05)之前
> - 挂回跑完:SO 602 key / PO 238 key,再跑 resolve 全部 already attached
> - 2026-09-02 重验:`upload-line-photos-r2.mjs MODE=verify` 抽 25 个
>   **byte-identical,0 missing,0 wrong,VERDICT: PASSED**;另外用 R2 REST API
>   查过那 840 个 key,**全部 200 image/jpeg,0 缺**
>
> **为什么留着划掉的原句而不是删掉:**这两行(和 `ac-resync-runbook.md` 阶段 3b
> 的同一句)在 2026-09-02 让 owner 收到一个错的答复 —— 「有 207 行的图可能显示
> 不出来」。那 207 行一直好好的。**过期的事实比没有事实更贵**,因为它读起来跟刚
> 量出来的一模一样。CLAUDE.md 里「a number in a comment is a fact with an expiry
> date」讲的就是这件事,这里再付一次钱。
