# Delivery Sheet Sync (ERP <-> HC Delivery Updated)

Feeds the dispatch team's working Google Sheet ("HC Delivery Updated": tabs *Delivery Details* = West Malaysia, *EM Order* = Sabah/Sarawak, *SG Order*) from the ERP instead of from AutoCount. Sibling pre-auth intake: `docs/modules/service-case.md`.

## Statuses and flow

- The sheet keys every row on col B (Doc. No.), rows from row 4. A pull writes three column blocks using AutoCount's original field names (so the existing Apps Script and sheet columns did not need to change shape) — a Total/status/customer block, an address/remark block, and an Attention/Sync-Status block.
- Col A (delivery message status) and columns Q..X are team-owned and are NEVER written by a pull — an `onEdit` in the sheet copies A→P and Q→O and marks the row PENDING; the next push sends PENDING rows back to the ERP.
- `GET /so-since?since=&limit=` returns orders modified strictly after `since` (oldest first, page size ≤1000, default 300); the checkpoint (`next_since`) is the last record's `LastModified` and must be stored only after every row of that page has been written.
- `POST /updates` writes back up to 300 rows per call as ONE batched `UPDATE ... FROM (VALUES ...)` statement — a present `Remark4` (including blank) is written, an absent one keeps the ERP's value, and a blank `ExpiryDate` KEEPS the ERP's date rather than nulling it (the prior AutoCount sync nulled text dates on a blank; this leg must not repeat that).
- DRAFT and CANCELLED orders are never sent — a cancelled order's row keeps whatever it last showed in the sheet.
- **Phase 2 (2026-09-16):** `GET /overdue` and `GET /balance-collection` are the two daily lists that used to come from AutoCount's `getOverdue` / `getBalanceCollection` — full state, no cursor, same record shape plus `SOUDF_VENUE`. Owner rulings: **Overdue** = an undelivered order (any live status other than DELIVERED / INVOICED / CLOSED, held ones included) whose `customer_delivery_date` is before today (Malaysian day), oldest first, no age cap (63 on 2026-09-16, 22 of them >90 days); the Apps Script appends them to the Overdue tab with a Pull Date and then pushes every one of them to today+3 through `POST /updates` (`ExpiryDate` only) — the AutoCount-era behaviour, kept on purpose. **Balance Collection** = a delivered order (DELIVERED / INVOICED / CLOSED) with `balance_sen_live > 0` (27 orders / RM 58,833 on 2026-09-16); the tab is rewritten whole. Every order on the old Balance Collection tab was absent from the ERP's outstanding import, which is how the AutoCount rule ("fully transferred, UDF_BALANCE > 0") was recovered. Delivered-but-owing orders from before go-live are not in the ERP and will not appear.
- The Apps Script APPENDS a row only for an order dated on/after `ERP_APPEND_FROM` (Script property, default `2026-09-15`); rows already on the sheet are always updated. AutoCount's getSince only ever added an order when it was modified, so the ~2,400 go-live-imported old orders (2023–2024, balance 0, "CANCELLED ORDER" remarks) had never reached the sheet — the first ERP pull, starting at the epoch, began appending them and timed out after 214 rows (Delivery Details R4243–R4456, SO-000021 … SO-003481; EM Order / SG Order none). Every pull's log line now carries "Skipped N old order(s) not on the sheet".
- **Phase 3 (2026-09-16):** the *Outstanding PO* tab. `GET /outstanding-po` replaces AutoCount's `/PurchaseOrder/getOutstanding`: one record per PO LINE with quantity still to receive (`qty - received_qty > 0`) on a PO in the PO list's own `outstanding` roll-up (SUBMITTED + PARTIALLY_RECEIVED, held ones included), this company only, oldest PO first (512 lines / 217 POs on 2026-09-16, every one with a book number). AutoCount field names again: `DocNo` = book number, `SODocNo` = the line's source order's book number (361 of 512 lines have one), `Location` = line warehouse else header warehouse, `DeliveryDate` = line date else `expected_at`, `SupplierDeliveryDate1/2/3` = HEADER `supplier_delivery_date_2/3/4` (= the book's `EDate / EDate2 / EDate3`). `POST /po-dates` replaces `/PurchaseOrder/update-udf-dates`: per PO, only a date that MOVED is written — through `cascadePoSupplierDate` (header + every line, audit row), the same writer as the PO editor's bulk supplier-date action and the line import — and then ONE `enqueueEdit` so the account book follows; a date the ERP already holds answers `unchanged` and queues nothing; a blank keeps the ERP's date (clear it in the ERP); a PO with a live GRN is `po_locked`, a PO no longer outstanding is `po_not_outstanding`. The tab layout is unchanged: rows 1–10 are the team's, header row 11, col P (Overdue Days) is manual and survives a refresh.

## Permissions

- Both routes are mounted PRE-AUTH and instead check a single shared secret header, `X-Intake-Key` = `SHEET_SYNC_KEY` — no session, no permission key. A wrong key answers 401 after a fixed delay, and 429 after repeated failures from one IP (the same rate-limit shape as the other pre-auth intake endpoints).

## Rules that must not break

- The sheet's `DocNo` must be `COALESCE(linked_ac_docno, doc_no)` — the sheet keys on the AutoCount document number, so a migrated order must be emitted under its AutoCount number (`ErpDocNo` carries the ERP's own number alongside it) or its row duplicates.
- The dispatch date (`SalesExemptionExpiryDate`, sheet col O) must be sourced from `customer_delivery_date`, never from the dead `sales_exemption_expiry` column or from `amended_delivery_date` — this is the specific date the owner chose for this feed.
- `Remark4` (sheet col P) is written back from the sheet's col A, but col A itself must never be written BY the ERP pull — it is the dispatch team's own field.
- `LastModified` must be the greatest of the order's own `updated_at`, its payments' `created_at`, and its delivery orders' `updated_at` — the header timestamp alone misses a large share of updates driven by a new collection.
- A pull must never blank a cell the ERP has no value for — the Apps Script preserves the sheet's existing value for any field the ERP returns as null, so an old migrated row keeps its AutoCount delivery-order number until the ERP actually raises one.
- `TransferTo` must be read from `scm.delivery_orders.do_number` (non-cancelled, comma-joined) — the header's own `transfer_to`/`linked_do_doc_no` columns are unused and always NULL.
- `Total`/`SOUDF_BALANCE` must be computed from `local_total_sen` (and payments), never from the base table's `balance_sen`, which is not the outstanding amount.
- A PO supplier date from the sheet must go through `cascadePoSupplierDate` + `poHasDownstream` + `enqueueEdit`, never a raw UPDATE: a raw write skips the lines, the audit row and the AutoCount edit, and the PO editor's own lock (a live GRN) would be bypassed. Only a changed date may queue an edit, or the daily push of the whole tab would send every outstanding PO to the book every morning.

## Gotchas

- LIVE since 2026-09-16 14:56: `ERPDeliverySync.gs` is in the live project "Delivery & Amend Updated", `erpSeedFromSheet()` ran (493 sheet rows matched an ERP order; the other 4,122 are pre-go-live delivered orders and service/pickup/fair rows), and `scheduledErpSync` fires every 15 minutes (push first, then pull). `reference/ERPDeliverySync.gs` is byte-identical to the live file (rebuilt from the recorded edits, hash-verified 2026-09-16).
- The 214 appended rows are still on Delivery Details until the owner runs `erpDeleteAppendedRows()` (deletes the block only when every row has col A empty and Sync Status SYNCED; `erpListAppendedRows()` is the read-only preview; `ERP_ROWS_BEFORE_FIRST_PULL` pins the seed-time counts 4238 / 243 / 134).
- The live `Main.gs` (208 lines) differs from `reference/Main.gs` (the 2026-04 export). Since 2026-09-16 (owner: 「把 Force Pull Missed SO 和退役的 AutoCount 函数都删掉」) the menu is `createMenu('ERP Sync')` with "Pull Latest FROM Houzs ERP" (`manualErpPull`), "Push Changes TO Houzs ERP" (`manualErpPush`), then the unchanged EM-transporter, Overdue, Balance, PO and 2990 items; the AutoCount items ("Pull Latest FROM AUTOCOUNT", "Push Changes TO AUTOCOUNT", "Force Pull Missed SO") and the functions `manualPull`, `manualPush`, `scheduledPull`, `scheduledPush` are DELETED, not retired. Two AutoCount triggers owned by a removed colleague ("Other user") cannot be deleted by anyone left on the project and DID run successfully on 2026-09-16 at 12:09 / 12:18 and 14:39 / 14:48; with their handler functions gone they now fail with "function not found" and touch nothing. `GetAutoCountData.gs`, `Diagnostic.gs promptForcePull` / `forcePullDoc` and `resetSyncTime` remain as dead code until phase 2 retires the other AutoCount pulls.
- The ERP has no processing date / delivery date for ~75% of the go-live-imported orders (2,876 migrated: `processing_date` on 682, `customer_delivery_date` on 704 — the import never mapped AutoCount's PDate / expiry into these columns). The sheet keeps its AutoCount values on those rows through the preserve rule; the owner declined seeding sheet col N (Processing Date) into the ERP (2026-09-16, 「不用」).
- Editing the live project: the function picker ignores synthetic clicks and keys — run a function by putting `if (true) return <fn>();` as the first line of the file's first function, Save, Run, remove, Save. A `monaco` `setValue` alone does not mark the project dirty (click into that file's editor, type a space and Backspace, Ctrl+S); the header's "Unsaved changes" can stay stale, so reload in another tab to prove a save. The "Add a file" name box loses typed text (rename `Untitled.gs` via its kebab menu).
- Phase 3 (Outstanding PO) is the last AutoCount leg: `reference/ERPOutstandingPo.gs` is the sheet side (`scheduledErpPoSync` daily 07:00 = push the tab's dates, then refresh; menu `manualErpPoPull` / `erpSyncSelectedPoDates`; `setupErpPhase3Triggers()`), replacing `PO_Outstanding.gs`, whose orphan `dailySyncAllPODates` trigger (removed colleague, 100% error on 2026-09-16) dies with "function not found" once that file is emptied. The Overdue and Balance Collection tabs had in fact been frozen since 2026-07-28: their AutoCount triggers belonged to the removed colleague and had failed on most runs since (not all — `runBalanceCollectionPull` still succeeded on 2026-09-16 at 17:27). **Phase 2 went LIVE 2026-09-16 17:33–17:35 after #4044 deployed:** `ERPOverdueBalance.gs` (= `reference/ERPOverdueBalance.gs`) is in the live project, `setupErpPhase2Triggers()` installed `scheduledErpOverdue` + `scheduledErpBalance` (daily 07:00), the menu items "Refresh Overdue Sheet" / "Refresh Balance Sheet" point at `manualErpOverdue` / `manualErpBalance`, and `Main.gs` lost `manualOverdue` / `manualBalance` / `scheduledOverdue`. First runs: Balance Collection rewritten with 27 rows (16 expired, 5 within 3 days); Overdue appended 63 rows at R82 and extended all 63 to 2026-09-19 (prod confirmed: 63 orders, overdue count now 0). The live `GetOverdueData.gs` and `GetBalanceCollection.gs` were emptied to a retirement comment (their `reference/` copies stay as the historical export) so the orphan AutoCount triggers (`scheduledOverdue`, `runBalanceCollectionPull`) now fail with "function not found" instead of overwriting the tabs.
- The AutoCount push of the expiry date (col O → the book's own `SalesExemptionExpiryDate` UDF) stops the moment this cutover is live — the ERP write-back does not send that header field to AutoCount, so AutoCount's own copy of that date will no longer update from this path.

## Where the code is

- `backend/src/routes/deliverySheetSync.ts` — the pre-auth routes (SO feed + write-back, overdue, balance collection, outstanding PO + PO dates).
- `backend/src/lib/delivery-sheet-feed.ts` — SO pull SQL, field mapper, parsers; `backend/src/lib/delivery-sheet-po-feed.ts` — the Outstanding PO SQL, record mapper and heads read.
- `backend/src/lib/intake-company.ts` — shared company resolution (also used by ASSR form intake).
- `reference/ERPDeliverySync.gs`, `reference/ERPOverdueBalance.gs`, `reference/ERPOutstandingPo.gs`, `reference/GetAutoCountData.gs`, `reference/PO_Outstanding.gs` — the Apps Script sides (ERP-fed regional tabs, ERP-fed Overdue / Balance Collection, ERP-fed Outstanding PO, and the two legacy AutoCount-fed originals).
